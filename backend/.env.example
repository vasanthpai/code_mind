PORT=5000

# MongoDB connection string
MONGODB_URI=mongodb://127.0.0.1:27017/PulsePlay

# Secret used to sign JWTs — use a long, random value
JWT_SECRET=

# Frontend origin allowed via CORS
CLIENT_URL=http://localhost:3000
